package com.tfg.securerouter.data.app.screens.sh_login.registry

import com.tfg.securerouter.data.app.screens.ScreenComponentModelDefault


class ShLoginScreenContentRegistry(sharedCache: MutableMap<String, Any>) {
    val modules: List<ScreenComponentModelDefault> = listOf(

    )
}