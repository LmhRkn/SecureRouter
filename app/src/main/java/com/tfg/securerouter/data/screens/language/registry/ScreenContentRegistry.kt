package com.tfg.securerouter.data.screens.language.registry

import com.tfg.securerouter.data.screens.ScreenComponentModelDefault


class LanguageScreenContentRegistry(sharedCache: MutableMap<String, Any>) {
    val modules: List<ScreenComponentModelDefault> = listOf(

    )
}