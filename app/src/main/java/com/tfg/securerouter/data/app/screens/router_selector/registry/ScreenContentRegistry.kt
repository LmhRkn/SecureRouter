package com.tfg.securerouter.data.app.screens.router_selector.registry

import com.tfg.securerouter.data.app.screens.ScreenComponentModelDefault


class RouterSelectorScreenContentRegistry(sharedCache: MutableMap<String, Any>) {
    val modules: List<ScreenComponentModelDefault> = listOf(

    )
}