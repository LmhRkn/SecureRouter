package com.tfg.securerouter.data.app.screens.language.registry

import com.tfg.securerouter.data.app.screens.ScreenComponentModelDefault


class LanguageScreenContentRegistry(sharedCache: MutableMap<String, Any>) {
    val modules: List<ScreenComponentModelDefault> = listOf(

    )
}